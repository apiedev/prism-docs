title: Prism Video Player
description: High-performance native video player for Unity with HLS/DASH streaming support
baseurl: "/prism-docs"
url: "https://apiedev.github.io"

theme: jekyll-theme-minimal

plugins:
  - jekyll-relative-links

relative_links:
  enabled: true
  collections: true

defaults:
  - scope:
      path: ""
    values:
      layout: default

exclude:
  - Gemfile
  - Gemfile.lock
  - node_modules
  - vendor
